function soma(núm1=0, núm2=0) {
    return núm1 + núm2
} 

console.log(soma(2, 1))

// esse = 0 (do function) quer dizer que se um dos números não for escrito no código, será lido como 0 (se isso não for colocado, virará NaN - Not a Number)
